这次实际完成了什么？
已完成
1. 任务状态清理
它做了：

任务	动作
#9	解除依赖/清阻塞
#14	解除依赖/真实验证转入 #21
#4	解除依赖，因为被 #16 覆盖
#21	标记完成，因为脚本已写好
这个状态清理基本合理。

2. 新增 selftest-phase-4.ts
脚本已写好，且注册到：

pnpm --filter @workspace/scripts run selftest:phase-4 9
它会验证：

promptTokens
costUsd
mode
innovationTypes
gapFit
context propagation
这些指标设计是对的。

3. 脚本类型检查通过
它后面又跑了一次：

pnpm @workspace/scripts
并说：

零类型错误，脚本已就绪
所以脚本本身应该没明显 TS 错误。

但还没完成什么？
最关键：脚本还没用真实 Cookie 跑
它给出的运行方式是：

API_TEST_COOKIE='connect.sid=你的cookie' \
pnpm --filter @workspace/scripts run selftest:phase-4 9
说明这个脚本需要登录 cookie。

目前执行记录里没有看到：

promptTokens = 19xxx
costUsd = ...
innovationTypes = ...
gapFit = ...
也没有看到真正调用 session 9 生成模型的结果。

所以：

#21 脚本开发完成
但 #21 端到端验证未完成
这有什么影响？
目前我们还不能确认：

#14 的真实 prompt token 是否 ≤20k；
Phase 4 gap hints 是否真的进入生成 prompt；
context innovationType 是否在真实模型中传播；
本次生成实际花了多少钱；
gapFit 是否从固定值变成真实计算；
生成后的 innovationMeta 是否完整。
这些才是 smoke test 的核心价值。

我对任务状态的判断
#21 不应该标成“完全完成”
更准确状态应该是：

#21 selftest script implemented
#21 runtime verification pending
如果任务系统只能标完成/未完成，那可以新建一个 follow-up：

Run Phase 4 smoke test with auth cookie on session 9
或者把 #21 重新打开直到真实跑完。

下一步该怎么做？
你现在要做的只有一件事
拿到登录 cookie，然后运行：

API_TEST_COOKIE='connect.sid=你的cookie' \
pnpm --filter @workspace/scripts run selftest:phase-4 9
或者让 Replit 在有登录态的环境里跑。

跑完后要看什么？
请它输出这几项：

sessionId
gapReportExists
landscapeVersion
coverageRate
generatedModelId
promptTokens
completionTokens
costUsd
modelUsed
estimatedTokens
mode
innovationTypes
gapFit
contextPropagation
warnings
验收标准：

promptTokens <= 22k
modelUsed = gpt-5.2
mode = analysis_only if coverage < 70%
gapFit != 固定 30（如果 gapReport 存在且匹配）
innovationTypes 合理
contextPropagation 不误打
关于“没有下游阻塞任务”
它说：

没有下游阻塞任务

这不完全准确。

业务上还有：

#2 显示跳过论文详情
#8 warning 消失
#12 评审持久化
#10 评审导出
只是它们可能不依赖 #21。

但从产品验收角度，#21 仍然是 Phase 4 的最后验证。

最终判断
这次可以认可的部分
selftest-phase-4 脚本已完成
任务依赖清理基本合理
脚本类型检查通过
还不能认可的部分
Phase 4 端到端 smoke test 尚未实际通过
#14 真实 token 尚未确认
我的建议
把当前状态记录为：

#21 script completed; runtime pending with auth cookie.
下一步不要继续新开发，先真正跑脚本。

一句话：脚本写好了，但 smoke test 还没跑；别把“写了测试”当成“通过测试”。